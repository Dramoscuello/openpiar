# OpenPiar — variables para Docker Compose
# Copia este archivo como .env y completalo.

# Credenciales de PostgreSQL (las mismas que en backend/.env)
DB_USER=openpiar_user
DB_PASSWORD=
DB_NAME=openpiar_db

# Puerto donde se expone el frontend (opcional, default: 80)
# FRONTEND_PORT=80
